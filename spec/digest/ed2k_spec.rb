require "spec_helper"

RSpec.describe Digest::Ed2k do
  it "has a version number" do
    expect(Digest::Ed2k::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
